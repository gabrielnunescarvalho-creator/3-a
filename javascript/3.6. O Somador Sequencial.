function somadorSequencial() {
    let acumulador = 0; 

    for (let i = 1; i <= 100; i++) {
        let resultadoAnterior = acumulador;
        acumulador += i;

        console.log(`${resultadoAnterior} + ${i} = ${acumulador}`);
    }
}

somadorSequencial();
